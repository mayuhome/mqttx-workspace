# Changesets

Hello and welcome! This folder has been setup by `@changesets/cli`.

When you make changes to a package in this repository, run `pnpm changeset` and answer the prompts to generate a markdown file detailing the change and what kind of version bump (patch / minor / major) is required.
